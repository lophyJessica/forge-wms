# 10-产品设计通用规范

## PC 端
- 表格分页默认 20 条
- 状态变更通过按钮触发，不允许直接改状态字段
- 危险操作(删除/作废)需二次确认弹窗
- 按钮不可用时隐藏，不展示灰色 disabled 态

## PDA 端
- 大字体、大按钮，适合手套操作
- 扫码优先于手动输入
- 关键操作语音+震动反馈
- 离线模式自动切换，网络恢复后自动同步

## 通用
- 日期 YYYY-MM-DD，时间 YYYY-MM-DD HH:mm:ss
- 金额千分位，2 位小数
- 空值展示 `-`
- 主数据不物理删除，统一"停用"
- 商品信息单据中快照存储
